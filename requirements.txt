# SANKALP – Core requirements (unchanged)
pandas>=2.0.0
streamlit>=1.35.0
neo4j>=5.0.0
scikit-learn>=1.3.0
python-dotenv>=0.19.0
altair>=5.0.0
groq>=1.0.0

# RAG – pinned to 2.2.2 (last release that doesn't require torchvision)
# torch CPU-only keeps the Docker image small (~500 MB vs ~3 GB with CUDA)
sentence-transformers==2.2.2
torch>=2.0.0,<3.0.0 --index-url https://download.pytorch.org/whl/cpu
faiss-cpu>=1.8.0
numpy>=1.24.0

# Automation scheduler
apscheduler>=3.10
streamlit-autorefresh

# ── NEW modules ────────────────────────────────────────────────────────────────

# MCP Server (stdio transport — for Claude Desktop)
mcp>=1.0.0

# MCP Server (HTTP/SSE transport — for URL-based connectors)
starlette>=0.27.0
uvicorn>=0.24.0

# Geospatial map
folium>=0.15.0
streamlit-folium>=0.18.0
